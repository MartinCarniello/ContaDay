<% flash.each do |key, value| %>
  <div class="alert-message float-right">
    <div class="alert alert-<%= key %>">
      <a href="#" class="close" data-dismiss="alert" aria-label="close"> ×</a>
      <%= value %>
    </div>
  </div>
<% end %>